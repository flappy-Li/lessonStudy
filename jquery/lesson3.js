/**
 *  @param jQuery (dom)操作
 * 
 * .html() => {
 *    类似innerHTML html()里可以自行配置 可以传函数
 *    特殊 {
 *      $().html() 取值 只会当前子元素的第一个
 *                 赋值 会 大换血 循环操每一项
 *    }
 * }
 * .text () => {
 *    和html功能类似 只不过他是添加文本 
 * }
 * 
 * 
 */