/**
 *  @param {NODE_API}
 *  
 *  net.createServe 服务端创建服务
 *  server.listen 监听服务地址
 *  server.on('listening', function () {}) 监听服务
 *  net.connect 客户端
 *  socket.write
 *  socket.end();
 *  server.on('connection', function (socket) {}) 监听浏览器链接 
 * 
 *  fs.readFileSync 文件解析
 * 
 * 
 * 
 *  http.createServe 创建HTTP服务
 *  url.parse 将request 转化成对象
 *  http.listen 监听HTTP端口
 */